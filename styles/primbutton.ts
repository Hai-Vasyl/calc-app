import { StyleSheet } from "react-native"

export default StyleSheet.create({
  wrapper: {
    alignItems: "center",
  },
  title: {
    fontSize: 25,
    paddingVertical: 15,
    textAlign: "center",
  },
})
